# Rust build artifacts
/target/
**/*.rs.bk
Cargo.lock.bak

# Local cache and staging (these live in .git/, but ignore at top-level too if leaked)
prov.db
prov.db-journal
prov-staging/

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*~

# Action build artifacts (generated by ncc; only `dist/` is committed)
action/node_modules/
action/.npm/

# Local secrets
.env
.env.local
